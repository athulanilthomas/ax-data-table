import { defineComponent, computed } from 'vue';

export const AXDataTable = defineComponent({
  name: 'AXDataTable',
  setup(props, { slots }) {
    return () => {};
  },
});

export type AXDataTable = InstanceType<typeof AXDataTable>;
